import { createIconSet } from 'react-native-vector-icons';
import customFontGlyph from '../../assets/fonts/icons.json';

const Icons = createIconSet(
  customFontGlyph,
  'icons',
  'icons.ttf',
);
export default Icons;
